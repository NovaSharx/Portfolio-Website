import { FC, ReactElement } from 'react';

const Navigation: FC = (): ReactElement => {
    return (
        <div id='navigation'>
            Navigation
        </div>
    )
}

export default Navigation;